/*
 A Deque (pronounced "deck", short for Double-Ended Queue) is a linear data structure
 that allows you to insert and delete elements from both the front and the back ends.
 Unlike a standard queue which follows strict First-In-First-Out (FIFO) rules,
 or a stack which follows Last-In-First-Out (LIFO) rules,
 a deque functions as a hybrid capable of acting as both.
 */

#include <iostream>
using namespace std;

int main() {
    cout << "A.";
    return 0;
}
